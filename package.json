{
  "name": "dziro-chrome-extension",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -cw",
    "pkg": "npm run build && zip -r dziro.zip ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.2",
    "@types/node": "^17.0.28",
    "rollup": "^2.70.2",
    "typescript": "^4.6.3"
  }
}
